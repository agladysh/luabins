-- Extend test suite (see TODOs at the end of test/test.lua).
-- Document both C and Lua API.
-- Add a C API test suite.
-- Add "make test" target, must check both C89 and C++98 builds, 
   both C and Lua tests.
-- Tag 0.1 and announce on Lua list.
-- Use own auto-growing char buffer instead of Lua stack on save.
